# Data Directory

This directory contains the SQLite database with photo metadata.

- `photos.db` - Main database file (committed to git)
- `*.db-journal`, `*.db-shm`, `*.db-wal` - Temp files (gitignored)
